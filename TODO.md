# TODO — Nivel técnico

No es para escalar a muchos usuarios; es para que el proyecto esté a la altura de una app "grande" en cuanto a calidad, robustez y mantenibilidad.

## Calidad de código

- [x] **ESLint + Prettier** — flat config, `eslint-plugin-astro` + `eslint-plugin-react-hooks` (solo reglas clásicas) + `eslint-plugin-jsx-a11y`. Ver `design.md`.
- [x] **Husky + lint-staged** — `eslint --fix` + `prettier --write` en pre-commit, solo sobre staged.
- [x] **GitHub Actions** (`.github/workflows/ci.yml`): format:check → lint → astro check → build, en cada push/PR a `main`.
- [x] **Vitest** para unit tests de lógica pura — `calculateAge`, `sortFilmography`/`dedupeByMovieId`/`toReleaseYear`, `toGender`, `recentSearches.ts` (17 tests). Config vía `getViteConfig` de `astro/config` (respeta el alias `@/` y env de Astro), `environment: "jsdom"` para los tests que tocan `localStorage`. `pnpm test` corre en CI justo antes del build.
- [x] **Playwright** E2E del flujo core: buscar → seguir → marcar vista → ver progreso (`tests-e2e/core-flow.spec.ts`). TMDB mockeado con un servidor de fixtures propio (`tests-e2e/mock-tmdb-server.mjs`, apuntado vía `TMDB_API_BASE_URL`) — nunca toca la API real ni gasta cuota. Auth/Firestore reales via emulador (usuario sembrado por REST en el Auth emulator, sign-in vía un bypass del popup de Google expuesto solo bajo `PUBLIC_USE_FIREBASE_EMULATOR`). `pnpm test:e2e` corre en su propio job de CI (`e2e` en `ci.yml`). Detalle completo en `design.md`.
- [x] **Firebase Emulator Suite** + tests de `firestore.rules` — `tests/firestore.rules.test.ts` (8 tests con `@firebase/rules-unit-testing`), cubre: usuario lee/escribe su propio doc, otro usuario no puede leer/escribir el ajeno, cliente sin auth no puede nada, `followedPeople` + `watchedMovies` anidado, y `watchlist`. `pnpm test:rules` (`firebase emulators:exec`) — separado de `pnpm test`/CI principal porque necesita JVM; corre en su propio job de CI (`firestore-rules` en `ci.yml`, `ubuntu-latest` ya trae Java).

## Observabilidad

- [x] **Vercel Analytics + Speed Insights** — montados en `Layout.astro`. Falta activarlos en el dashboard de Vercel para que empiecen a recolectar.
- [x] **Sentry activo en producción** — `PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` configurados en Vercel. Verificado 2026-09-06: DSN real embebido en el bundle de prod, `release` coincide con el commit desplegado (sourcemaps subiéndose correctamente), evento de prueba enviado directo al ingest endpoint → 200 OK.
- [x] **Structured logging** en los endpoints `/api/*` — `logApiError()` en `src/lib/api.ts`: los 4 endpoints, al capturar un `TmdbError`, ahora emiten una línea JSON (`{level, route, message}`) a stdout (legible en Vercel function logs) y la reportan a Sentry (`Sentry.captureException`) antes de devolver el 502 genérico al cliente — la causa real ya no se pierde. Verificado en dev forzando una API key inválida: el log mostró `{"level":"error","route":"search-person","message":"TMDB request failed: 401 /search/person"}`.

## Performance

- [x] Chunk de >500kB — era Firebase mezclado con código UI compartido. Aislado en su propio chunk vía `manualChunks`. Ver `design.md`.
- [x] `srcset` responsivo en imágenes de TMDB — `src/lib/tmdb/image.ts` (density para tamaño fijo, width-based para grids/fluid).
- [x] `Cache-Control` en los endpoints `/api/*` (compartido en el CDN de Vercel entre usuarios, no solo por navegador).
- [x] Lighthouse CI — corre semanal/manual contra producción (`.github/workflows/lighthouse.yml`), en modo informativo (warn, no bloquea).
- [x] **Concurrencia acotada en fetches por-persona/por-película** (2026-09-14) — `mapWithConcurrency` (pool de 6) en `Dashboard.tsx`/`WrappedStats.tsx`/`ConnectionsPage.tsx`/`WatchlistPage.tsx`, en vez de disparar todo a la vez (chocaba con el rate limit) o uno por uno (lento con muchas personas/películas). Rate limit de `/api/person` y `/api/movie` subido 30→120/60s. Ver design.md.
- [ ] `client:idle`/`client:visible` en más islas si algún día se siente lento — por ahora solo `UserMenu` lo necesitaba; el resto es contenido primario de cada página (diferirlo perjudicaría la interacción real, no la ayudaría).
- [x] **Optimización de performance (2026-09-04)** — revisados: bundle de Sentry (encontrado y arreglado: Session Replay se bundleaba sin usarse, 271kB→85kB en el chunk cliente), prioridad de carga de imágenes (`TrendingMovies` tenía la fila entera en `lazy`, incluida la candidata a LCP), fuentes (ya óptimo, `unicode-range` por subset), CSS del skeleton (falso positivo, es Tailwind completo, tamaño normal), gsap del hero (no diferible, la animación de entrada lo necesita al montar), Firebase eager-load (evaluado, sin ganancia real sin cambio de arquitectura server-side). Detalle en `design.md`, sección Performance.
- [x] **Investigado el score de Lighthouse contra prod real** — las sospechas originales (cold-start serverless, CSS bloqueante) no se confirmaron: TTFB actual es ~80ms, nada de "durmiendo". `/` (0.88–0.95) y `/search` (0.99) ya estaban bien; el real problema era **`/watchlist` en 0.71 por CLS ~0.96** (shift enorme, casi el peor posible). Causa: `WatchlistPage.tsx` mostraba el mismo skeleton alto (grid de 8 posters) tanto mientras resolvía el auth check como mientras cargaba el watchlist — para un visitante anónimo (el caso común, y el que ve Lighthouse), ese skeleton alto colapsaba de golpe al mensaje corto "Sign in to keep movies on your radar", el shift más grande de toda la página. Fix: separar el estado "resolviendo auth" (skeleton chico, del tamaño del mensaje de sign-in) del estado "hay usuario, cargando películas" (el skeleton alto, que ahora solo se muestra cuando sabemos que sí hay contenido real por venir). Verificado contra prod post-deploy: 0.71→0.96 performance, CLS 0.96→0.038.

## Seguridad

- [x] **Rate limiting** en los 4 endpoints `/api/*` — 30 req/min por IP, en memoria (`src/lib/rateLimit.ts`). Ver `design.md`.
- [x] Cabeceras de seguridad (CSP, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`) vía `src/middleware.ts`. Login con Google confirmado funcionando en producción tras el cambio.
- [x] Auditoría de dependencias automática — `.github/dependabot.yml` (npm semanal + GitHub Actions).
- [ ] **Firebase App Check** — hoy cualquiera con la `PUBLIC_FIREBASE_API_KEY` (pública por diseño, pero aun así) podría golpear Firestore directo si no hay reglas perfectas. App Check añade una capa de "esto viene de mi app real". **Bloqueado en ti**: requiere que registres una site key de reCAPTCHA v3 en tu cuenta de Google y actives "Enforce" en la consola de Firebase — son cuentas/consolas tuyas, no se puede completar desde el código ni con las credenciales que ya tengo.

## Producto

- [x] **Seguir a otros usuarios** (2026-09-14) — revierte a propósito la postura "sin features sociales" de abajo (ver design.md, sección Social, para el razonamiento). `/u/{userId}`: perfil público (nombre/foto) con botón de solicitar seguir → aprobar/rechazar (inbox en el propio perfil) → una vez aprobado, ves su watched y watchlist (antes privados); favoritos (tablón de recomendaciones) ya era público y sigue igual. Sin buscador de usuarios — descubrimiento solo por link compartido, mismo criterio que el tablón. `UserMenu.tsx` ahora sincroniza un doc público `users/{userId}` (nombre/foto) al iniciar sesión y linkea a "My profile" con contador de solicitudes pendientes. **Recordatorio**: tocó `firestore.rules` (subcolecciones `followRequests`/`followers`/`following` nuevas, y `watchlist`/`seen` ahora legibles por seguidores aprobados) — publicar manualmente.
- [x] **Ruta propia para Movie Map + acceso desde cualquier card** (2026-09-14) — `/map` (antes solo una pestaña dentro de `/connections`, que se mantiene). Acepta `?movie={tmdbId}` para abrir directo centrado en una película. `MovieDetailsDialog.tsx` (que ya abre cada card de película en toda la app) tiene un botón "View in Movie Map" que linkea ahí — cubre "desde cualquier card" sin tocar cada componente de card por separado.

## Producto (histórico: "sin agregar features sociales" — ver excepción arriba)

- [x] **Dark mode** — toggle (`ThemeToggle.tsx`) + persistencia en localStorage + script anti-flash. Ver `design.md`.
- [x] **PWA** instalable con caché offline de lo ya visto — `public/sw.js` (manual, sin Workbox — `@vite-pwa/astro` no compila en este stack, ver `design.md`) + `public/manifest.webmanifest`. Ícono generado desde cero (bookmark blanco/negro, no había logo).
- [x] **Exportar/backup de datos** — "Export data" en el menú de usuario, descarga un JSON con personas seguidas, películas vistas y watchlist (`exportUserData` en `firestore.ts`).
- [x] Accesibilidad — foco visible (`.focus-ring` en `global.css`) en todos los interactivos que no pasan por el `Button` de shadcn (nav, cards, checkbox de película, link "via" del watchlist); `aria-live` (`src/lib/a11y.ts` + `#a11y-announcer`) en marcar visto, follow/unfollow, watchlist; auditoría `axe-core` semanal/manual contra producción (`.github/workflows/accessibility.yml`, informativo).
- [x] **Pase de accesibilidad + mobile (2026-09-07)** — auditoría con `axe-core` contra prod: 0 violaciones tras arreglar contraste del pie de página (`opacity-70` sacado) y corregir un falso positivo del modal de onboarding en el audit automático (`--load-delay`). Nav: íconos y toggle de tema pasan de ~28-32px a 44px (mejor touch target en mobile); agregado skip-link (WCAG 2.4.1, no existía). Detalle en `design.md`, sección Accesibilidad.
- [x] **Recent searches borrables** — botón "x" por ítem en el grid de `/search` (`removeRecentSearch` en `recentSearches.ts`).
- [x] **Home con contenido cuando no hay sesión** — fila "Trending this week" (`getTrendingMovies()`, `/trending/movie/week` de TMDB) en el estado sin login del Dashboard. Fetch server-side en `index.astro` (mismo patrón que `/person/[id]`, sin pasar por `/api/*` — es contenido de página, no algo que un cliente pida por su cuenta), pasado como prop a `<Dashboard>`. Clic en un poster abre el mismo `MovieDetailsDialog` que ya usa la filmografía. Si TMDB falla, la home igual carga (`TmdbError` capturado, fila vacía).
- [x] **Tablón público de recomendaciones** (2026-09-09) — `/board/{userId}`, sin login para verlo (pensado para compartir en redes, no es una feature social dentro de la app: nadie ve el tablón de otro usuario logueado a menos que le pasen el link). Botón de estrella en `MovieDetailsDialog` (separado del watchlist) para agregar/quitar. El dueño, con sesión, ve además un botón "Copy link". **Recordatorio**: este cambio tocó `firestore.rules` (subcolección `recommendations`, con lectura pública) — hay que publicarlo manualmente, ver sección de arriba.
- [x] **Búsqueda de películas** (2026-09-06) — `/search` ahora tiene un toggle "Actors & directors" / "Movies" (`SearchTabs.tsx`) para no mezclar resultados. Buscar una película abre su cast (links a la filmografía de cada actor, componente ya existente `MovieDetailsDialog`) y permite agregarla directo al watchlist (`MovieWatchlistButton.tsx`) sin pasar por la filmografía de nadie. Nuevo endpoint `/api/search-movie`. Ver `design.md`.
- [x] **Engagement: badges + nudges + Wrapped** — `src/config/engagement.json` prende/apaga cada feature. Badges persistidos en `users/{userId}/badges` (ver `design.md`): completar una filmografía, hitos de N filmografías (3/10/25), hitos de watchlist (10/25/50), primer actor/director completado, "retrospectiva completa" (5+ décadas vistas de una persona). Nudges: "N para completar" en la card (cuando quedan ≤3) y sección "Almost there" en el dashboard. `/wrapped`: total visto, persona más vista, década favorita — reutiliza el fetch por-persona que ya existía, sin requests nuevas a TMDB. "On this day" en `/person/[id]` cuando alguna película de esa filmografía se estrenó un día como hoy. **Recordatorio**: este cambio tocó `firestore.rules` (subcolección `badges` nueva) — hay que publicarlo manualmente, ver sección de arriba.
- [x] **Compartir badge como imagen** (2026-09-10) — botón de share en cada badge del dashboard (`ShareBadgeButton.tsx`), genera un PNG cuadrado de marca (Canvas API nativo, sin dependencia nueva — `src/lib/shareBadgeImage.ts`) y lo pasa a `navigator.share` (fallback: descarga). Cierra el loop de los badges, que hoy se ganaban una vez y quedaban muertos en el dashboard; mismo mecanismo barato de crecimiento orgánico que el tablón público.
- [x] **Alertas de estreno** (2026-09-10) — cron diario (`vercel.json`) contra `/api/cron/check-releases`, notifica en una campanita in-app (`NotificationBell.tsx`) cuando una persona seguida saca película nueva. Usa Firebase Admin SDK para leer `followedPeople` de todos los usuarios. **Bloqueado en el usuario**: cargar `FIREBASE_SERVICE_ACCOUNT` y `CRON_SECRET` en las env vars de Vercel (ver design.md, sección Engagement) — sin eso el cron no funciona en producción. También toca `firestore.rules` (subcolección `notifications` nueva) — publicar manualmente.
- [x] **Página "Watched"** (2026-09-14) — `/watched`, todo lo marcado como visto sin importar de dónde (filmografía, búsqueda, watchlist, board, Connections), filtro por género, agrupado por año o por persona. Vinculada desde el nav (desktop y mobile). Ver design.md.
- [x] **Sistema unificado de acciones watched/watchlist** (2026-09-14) — `<MovieActions>` (`src/components/movies/MovieActions.tsx`) reemplaza `MovieSeenButton`/`MovieWatchlistButton` (borrados) y las variantes ad-hoc con `Button`+icono repetidas en cada página: mismo cluster (ojo+bookmark), tooltip (`src/components/ui/tooltip.tsx`, nuevo — envuelve `@base-ui/react/tooltip`) y estado activo (`bg-primary`, el acento ámbar ya establecido, no rojo) en filmografía, watchlist, resultados de búsqueda, trending, co-stars de Connections, tablón público y el diálogo de detalle. `useMovieActionState` (`src/lib/hooks/useMovieActionState.ts`) centraliza el toggle optimista + auth-gate para páginas sin un `Set` en bloque ya cargado (antes esa lógica vivía duplicada en cada botón); páginas que ya tienen el estado en bloque (Watchlist, Filmography) usan `<MovieActions>` controlado. **Bug arreglado de paso**: `public/sw.js` rechazaba el `respondWith` cuando una página no cacheada fallaba en red — Safari mostraba "FetchEvent.respondWith received an error" y no cargaba nada; ahora siempre resuelve a una `Response`.
- [x] **Slider "New on Pelicoolas" en el home** (2026-09-14) — `RecentUsersSlider.tsx`, query pública `orderBy(createdAt, desc)` sobre `users` (ya es público-read). `PublicProfile.createdAt` se setea solo la primera vez que `syncPublicProfile` corre para ese uid (chequea el campo, no la existencia del doc, para poder backfillear cuentas creadas antes de este campo) — nunca se pisa en logins posteriores. Se excluye al propio usuario de la lista. Cada avatar linkea a `/u/{uid}` para pedir seguir.
- [x] **Toggle grid/lista en `/watched` y `/filmographies`** (2026-09-14) — mismo patrón que watchlist: estado `viewMode` persistido en `localStorage`, aplicado dentro de cada agrupación existente (`MovieGroup` en `WatchedPage.tsx` sirve grid o lista sin duplicar la lógica de agrupación por año/persona). `/watched` también reemplazó el texto "N of N movies watched" por el conteo por género dentro de cada pill. `FollowedPersonCard` gana un `layout="list"` para `/filmographies` (el toggle no se muestra en la vista resumida de la home, solo en la página completa).
- [x] **Rediseño de `/watchlist`** (2026-09-14) — header con stats + barra de progreso (`FilmographyProgress`, reusado), tabs Todas/Por ver/Vistas (estado calculado intersectando contra `seen`, no un campo propio — mismo patrón que el resto de la app), select de orden (recencia/rating/A-Z, `shadcn add select`), toggle grid/lista persistido en `localStorage`, botón "Pick something for me" (elige al azar entre lo no visto, sin requests nuevos — usa la lista ya cargada), grid más denso (`grid-cols-2..6` real en vez de masonry `columns-*`, necesario para alinear badge/duración/género entre cards) y cards con badge de estado, duración y género. `durationMinutes` se agrega a `WatchlistMovie` y se backfillea en el mismo `fetchMovieDetails` que ya traía los géneros (`setWatchlistDetails` reemplaza a `setWatchlistGenres`, un solo `updateDoc` para ambos campos) — cero requests TMDB adicionales. Ver design.md.
- [x] **Invitar por email** (2026-09-15) — `UserMenu` → "Invite a friend" (`InviteDialog.tsx`), `POST /api/invite` verifica el ID token del que invita (`getAdminAuth().verifyIdToken`), rate-limit 10/día por usuario, envía vía Resend (dominio `pelicoolas.com`, Vercel Marketplace). Tracking de conversión: el link (`?invite={id}&ref={inviterUid}`) se guarda en `localStorage` (`inviteTracking.ts`, sobrevive el redirect de auth), y si `syncPublicProfile` detecta un usuario nuevo, `POST /api/invite/convert` marca el invite como `converted` — todo vía Admin SDK (mismo patrón privilegiado que el cron de estrenos), así ningún usuario necesita permiso de escritura sobre el invite de otro y `firestore.rules` deja `invites` en owner-read-only sin reglas cruzadas riesgosas. **Bloqueado en el usuario**: verificar el dominio `pelicoolas.com` en el dashboard de Resend (SPF/DKIM) antes de que el envío real funcione. También toca `firestore.rules` (subcolección `invites` nueva) — publicar manualmente.
- [ ] **i18n ES/EN (fase 1 — fundación, 2026-09-15)** — `src/i18n/en.ts`/`es.ts`: diccionario tipado, `es.ts` declarado `: Dictionary` (tipo derivado de `en.ts`) así una key faltante/sobrante o una función de interpolación con firma distinta es error de `astro check`, no bug en runtime — sin necesidad de test de paridad aparte. `middleware.ts` resuelve el locale por request: cookie `locale` (elección explícita) si existe, si no `Accept-Language` del browser, si no `en` por defecto — expuesto en `Astro.locals.locale` (tipado en `env.d.ts`). `LocaleToggle.tsx` (nuevo, al lado de `ThemeToggle` en el header) cambia el idioma con reload completo (a diferencia de `ThemeToggle`, el locale cambia lo que el server renderiza, no es solo CSS — reload es lo más simple para no duplicar lógica de re-traducción en cliente). Migrado en esta pasada: `Layout.astro` (nav, footer, `<html lang>`), `UserMenu.tsx`, `LoginButton.tsx`, `MobileNav.tsx`, `ThemeToggle.tsx`, `OnboardingCarousel.tsx` (el único componente que ya estaba 100% en español). **Pendiente (fase 2, no en esta pasada)**: el resto de ~55 componentes/12 páginas siguen en inglés hardcodeado (Dashboard, Filmography, Watchlist, Watched, Connections, social, badges, `/privacy`) — migrar componente por componente reusando `getDictionary(locale)`. **Fuera de alcance explícito**: datos de TMDB (títulos/sinopsis) siempre vienen en inglés — nunca se pasó `language=es-ES` a la API; localizarlos requiere versionar el cache de `movieData.ts`/`clientCache.ts` y el `Cache-Control` de `/api/*` por idioma, evaluar aparte. Emails de Resend (`welcome-email`, `invitation-email`) siguen solo en español — requeriría duplicar templates en el dashboard de Resend.
- [x] **SEO** — `astro.config.mjs` con `site` (necesario para URLs absolutas). `Layout.astro` genera por página: `<meta name="description">`, canonical, Open Graph + Twitter Card (imagen real del actor vía TMDB en `/person/[id]`, ícono de la app como fallback), y `<meta name="robots">`. `/`, `/filmographies` y `/watchlist` van `noindex` — son dashboards que exigen login y no muestran nada distinto a un crawler anónimo salvo un botón de sign-in (contenido fino/duplicado, `/filmographies` además es literalmente el mismo componente que `/`, con canonical apuntando a `/`). `/search` y `/person/[id]` sí indexables — son el contenido real y público del sitio. `public/robots.txt` permite todo salvo `/api/`. **Sin sitemap.xml a propósito**: las únicas páginas estáticas indexables son `/` (que está noindex) y `/search`; el contenido real está en `/person/{id}` con IDs de TMDB — no hay forma de enumerarlos sin consultar la API completa de TMDB, así que un sitemap real no es viable sin sobre-ingeniería.

## Diseño visual / UI — auditoría 2026-09-09 (sin implementar, solo relevado)

Repasé la app entera (home, `/search`, `/person/[id]`, `/watchlist`, `/connections`, `/board`) en desktop y mobile, claro y oscuro. Conclusión: funcionalmente está fuerte, pero visualmente se siente genérica — es básicamente el theme "neutral" de shadcn sin tocar. Ítems concretos, de mayor a menor impacto:

- [x] **Paleta 100% monocromática — cero color en toda la app** (2026-09-10) — `--primary`/`--primary-foreground` (claro y oscuro) pasaron de `oklch(X 0 0)` a un acento dorado/ámbar (`oklch(0.52 0.16 45)` claro, `oklch(0.78 0.15 65)` oscuro), contraste verificado ≥5.7:1. Recolorea Button/Badge/Progress/Checkbox por defecto en toda la app vía ese único token. `--ring` (foco) se dejó sin tocar a propósito.
- [x] **Sin profundidad/elevación** (2026-09-10) — utilidad compartida `.card-elevated` (`global.css`: lift + shadow al hover) aplicada a todas las cards de póster/filas de resultado (filmografía, watchlist, trending, tablón de recomendaciones, connections, resultados de búsqueda).
- [x] **`/search` se ve vacía** (2026-09-10) — reutiliza `TrendingMovies` (mismo fetch server-side que la home) como contenido de fondo mientras no hay query, en ambas pestañas (actores/películas).
- [x] **Tipografía de una sola nota** (2026-09-10) — h1 "hero" (home sin sesión, `/search`, `PersonHeader`) pasaron de `text-xl/2xl font-semibold` a `text-3xl sm:text-4xl font-bold tracking-tight`. El resto (dashboards/utilitario) se dejó igual a propósito.
- [x] **Placeholder de poster faltante en la lista de filmografía** (2026-09-09, arreglado junto con el rediseño de arriba) — `MovieItem.tsx` ya tiene la misma rama `else` con "No poster" que `WatchlistPage`/`TrendingMovies`/`ConnectionsPage`.
- [x] **La lista de filmografía es más "checklist" que "vitrina"** (2026-09-09) — `MovieItem.tsx` pasó de fila angosta (poster 40x56) a tarjeta de póster grande, mismo lenguaje visual que el watchlist: rating arriba-izquierda, watchlist arriba-derecha, checkbox de vista abajo-izquierda sobre la imagen, grid tipo masonry (`columns-2/3/4`) en vez de lista apilada. Vista = poster atenuado (`opacity-40`), da progreso legible de un vistazo en todo el grid, no solo por ítem. De paso, agregado el placeholder "No poster" que faltaba (otro ítem de esta misma auditoría).
- [x] **Micro-interacciones limitadas a un solo lugar** (2026-09-10) — cubierto por `.card-elevated` de arriba: transición CSS simple (lift + sombra) en cards de póster/filas en toda la app, sin GSAP nuevo.

## Arquitectura / DX

- [x] **Tipos de respuesta TMDB compartidos + validación en runtime** — `src/types/tmdb.ts` define un schema Zod por endpoint (`tmdbSearchPersonResponseSchema`, etc.), con el tipo inferido del propio schema en vez de una interface separada. `tmdbFetch` ahora recibe el schema y hace `safeParse`; si TMDB cambia el shape, `TmdbError` explota en el momento en vez de propagar `undefined` silenciosamente a la UI.
- [ ] Documentar en `design.md` el porqué de cada decisión no obvia (ya iniciado) — mantenerlo vivo cada vez que se tome una decisión de arquitectura nueva.

## Prioridad sugerida si hay que elegir por dónde seguir

No hay ítem bloqueante pendiente. Auditoría visual 2026-09-09 completa (todos los ítems implementados 2026-09-09/10). Único pendiente real de todo el documento: **Firebase App Check**, bloqueado en el usuario (requiere consola/cuenta de Google propia).
